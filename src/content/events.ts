import type { GameEvent } from "../sim/types";

// Event system is wired but intentionally empty for now — the generic
// placeholder events weren't serving a gameplay purpose yet. New events
// should be added with a clear trigger and narrative hook.
export const EVENTS: GameEvent[] = [];
